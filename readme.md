* Start
* set life available for user to guess 
* Computer_num  (number btw 1 and 100)
* collect User_guess (number btw 1 and 100)
* if life is exhausted end game and ask if the user wants to start again
* if user_guess is equal computer_guess (user win, end game) and ask if the user wants to start again.
* if user_guess < computer_num, print guess is bigger than number (user continue to guess until life is over)
* if user_guess > computer
* if user_guess > computer_num, print guess is smaller than number (user continue to guess until life is over
